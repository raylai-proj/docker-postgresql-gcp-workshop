[project]
name = "02-workflow-orchestration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chun Juei (Ray) Lai", email = "chunjueilai@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
02-workflow-orchestration = "02_workflow_orchestration:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
