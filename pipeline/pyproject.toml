[project]
name = "pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "pyarrow>=24.0.0",
]

[dependency-groups]
dev = [
    "pgcli>=4.5.0",
    "psycopg[binary]>=3.3.4",
]
